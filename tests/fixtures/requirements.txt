# Test file with known vulnerable packages
# DO NOT install these - for testing scanner only

# Django with known CVE (SQL Injection, XSS, etc.)
django==2.2.0

# Flask with known CVE
flask==0.12.0

# Requests with known CVE
requests==2.19.0

# Jinja2 with known CVE (sandbox escape)
jinja2==2.10.0

# PyYAML with known CVE (arbitrary code execution)
pyyaml==5.1.0

# Pillow with known CVE
pillow==6.0.0

# Urllib3 with known CVE
urllib3==1.24.0

# Cryptography with known CVE
cryptography==2.8
